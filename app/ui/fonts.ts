import { Inter, Questrial, Lusitana } from 'next/font/google';


export const questrial = Questrial({ weight: ['400'], subsets: ['latin'] });
export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
  });

